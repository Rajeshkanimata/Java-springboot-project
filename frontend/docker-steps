#create a app.py and requirment.txt
 # In frontend folder and there you need to have Dockerfile and 
// FROM python:3.10

// # Create app directory
// WORKDIR /app

// # Install pip dependencies
// RUN pip install --no-cache-dir \
//     streamlit \
//     requests

// # Copy your frontend code
// COPY . .

// # Expose Streamlit port
// EXPOSE 8501

// # Set default backend/API URL
// # (You override this at runtime)
// ENV API_URL=""

// # Run the Streamlit app
// CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]

  command to run this
  
  docker run -d -p 8501:8501   -e API_URL="http://98.93.27.4:8082" frontend this is my command replace with your backend ip

    -e to pass enivirorment backend ip thorugh docker command 

    don't pass backedn ip in dockerfile 

    using docker we can skip systemd
